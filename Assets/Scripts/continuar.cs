using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class continuar : MonoBehaviour
{
    private bool isPaused = false;

    //public Pause continuar;

  

    void TogglePause()
    {
        //continuar.isPaused = !isPaused;

        if (isPaused)
        {
            Time.timeScale = 0f; // ???????????? ?????
            // ?????????????? ???????? ??? ????? (??????????? ???? ????? ? ?.?.)
        }
        else
        {
            Time.timeScale = 1f; // ???????????? ?????
            // ?????????????? ???????? ??? ??????????? ???? (??????? ???? ????? ? ?.?.)
        }
    }
}
